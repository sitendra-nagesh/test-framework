site-packages are specific user packages. They allow an individual user to install packages without admin privileges locally. 
Run below command to check the location of site packages, 
python -m site --user-site
Google for not using the user specific packages. 
